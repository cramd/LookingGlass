all:
  children:
    shiftmon:
      hosts:
        monitoring:
          # ShiftMon LXC on PVE2 (192.168.0.90 — the beefier box)
          ansible_host: 192.168.0.100
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519  # adjust if needed
